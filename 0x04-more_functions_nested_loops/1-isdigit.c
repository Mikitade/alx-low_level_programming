#include "main.h"

/**
 * _isdigit - checks for character that are digit
 * @c: character
 * Return: success
 *
 * Return 0 or 1
 */

int _isdigit(int c)
{
	if (c >= '0' && c <= '0')
		return (0);
	else
		return (1);
}
