#include <stdio.h>

/**
 * main - causes an infite loop
 * Return: 0
 */

int main(void)
{
	int i;

	printf("infinite loop incomming :(\n");

			i = 0;
			/*
			 * while (i < 10)
			 * {
			 *                putchar(10);
			 * }
			 */

			printf("Infinite loop avoided! \\o\n");

			return (0);
}
